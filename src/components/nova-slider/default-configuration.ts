enum AXIS {
  HORIZONTAL =  "horizontal",
  VERTICAL = "vertical"
}

enum MODE {
  CAROUSEL = "carousel",
  GALLERY = "gallery"
}

enum VARIANT {
  CARDS = "cards"
}

enum AUTOPLAY_DIRECTION {
  FORWARD = "forward",
  BACKWARD = "backward"
}

enum PREVENT_SCROLL_ON_TOUCH {
  AUTO = "auto",
  FORCE = "force"
}

enum NESTED {
  INNER = "inner",
  OUTTER = "outer"
}

const ANIMATE_IN = "tns-fadeIn";
const ANIMATE_OUT = "tns-fadeOut";
const ANIMATE_NORMAL = "tns-normal";

export {
  AXIS,
  ANIMATE_IN,
  ANIMATE_OUT,
  ANIMATE_NORMAL,
  AUTOPLAY_DIRECTION,
  PREVENT_SCROLL_ON_TOUCH,
  NESTED,
  MODE,
  VARIANT
}

